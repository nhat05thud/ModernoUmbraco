@inherits UmbracoViewPage<Moderno.Web.Models.ProjectItems>

@foreach (var item in Model.LstItems)
{
    var lstItem = new List<int> { 0, 1, 2, 3 };
    var thumb = Umbraco.TypedMedia(item.GetPropertyValue<int>("image"));
    <div class="item @(lstItem.Contains(Model.LstItems.IndexOf(item)) ? "item-1" : "item-2")">
        <a href="@item.Url">
            @if (lstItem.Contains(Model.LstItems.IndexOf(item)))
            {
                <img class="lazyload" data-original="@(thumb != null ? thumb.GetCropUrl(360, 325, furtherOptions: "&slimmage=true") : "/images/no-image.png?anchor=center&mode=BoxPad&width=360&height=325&bgcolor=fff&slimmage=true&rnd=" + item.UpdateDate.ToString("ddMMyyyhhmmss"))" alt="" />
            }
            else
            {
                <img class="lazyload" data-original="@(thumb != null ? thumb.GetCropUrl(555, 500, furtherOptions: "&slimmage=true") : "/images/no-image.png?anchor=center&mode=BoxPad&width=555&height=500&bgcolor=fff&slimmage=true&rnd=" + item.UpdateDate.ToString("ddMMyyyhhmmss"))" alt="" />
            }
            <div class="content">
                <h1>@item.Name</h1>
                <div class="desc">
                    <span>@item.GetPropertyValue("location")</span> | <span>@item.GetPropertyValue("acreage")</span>
                </div>
            </div>
        </a>
    </div>
}